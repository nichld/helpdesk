<div class="flex flex-col items-center justify-center max-w-md mx-auto p-6 bg-white rounded-lg border border-gray-200 shadow-sm">
  <div class="text-center mb-6">
    <svg class="mx-auto h-16 w-16 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
    </svg>
    
    <h1 class="mt-4 text-2xl font-bold text-gray-900">Your account is pending approval</h1>
    <p class="mt-2 text-gray-600">
      Thank you for registering, <%= user.firstName %>. Your account needs to be approved by an administrator before you can access the system.
    </p>
  </div>
  
  <div class="w-full bg-yellow-50 border border-yellow-200 p-4 rounded-md text-sm text-yellow-800 mb-6">
    <div class="flex">
      <svg class="h-5 w-5 text-yellow-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <div>
        <p class="font-medium">What happens next?</p>
        <p class="mt-1">An administrator will review your account details and approve your access. You'll be able to log in once your account is approved.</p>
      </div>
    </div>
  </div>
  
  <div class="w-full mb-6">
    <h2 class="text-lg font-medium mb-2">Your account details:</h2>
    <div class="bg-gray-50 p-4 rounded-md border border-gray-200">
      <div class="grid grid-cols-2 gap-4 text-sm">
        <div>
          <p class="text-gray-500">Name</p>
          <p class="font-medium"><%= user.firstName %> <%= user.lastName %></p>
        </div>
        <div>
          <p class="text-gray-500">Email</p>
          <p class="font-medium"><%= user.email %></p>
        </div>
        <div>
          <p class="text-gray-500">Role</p>
          <p class="font-medium">
            <%= user.role === 'customer' ? 'Customer' : 
               user.role === 'employee_technical' ? 'Technical Support' :
               user.role === 'employee_billing' ? 'Billing Support' :
               user.role === 'employee_general' ? 'General Support' :
               user.role === 'admin' ? 'Administrator' : 
               user.role %>
          </p>
        </div>
      </div>
    </div>
  </div>
  
  <div class="flex w-full justify-between">
    <a href="/" class="text-blue-600 hover:text-blue-800 text-sm">Return to homepage</a>
    <form action="/logout" method="POST">
      <button type="submit" class="text-blue-600 hover:text-blue-800 text-sm">Log out</button>
    </form>
  </div>
</div>

<script>
  // Check approval status periodically (every 60 seconds)
  setInterval(() => {
    fetch('/check-approval', { credentials: 'same-origin' })
      .then(response => response.json())
      .then(data => {
        if (data.approved) {
          window.location.href = '/profile';
        }
      })
      .catch(error => console.error('Error checking approval status:', error));
  }, 60000);
</script>
